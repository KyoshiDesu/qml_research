name: qsvm-vm-compare
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - numpy
  - scikit-learn
  - psutil=5.9.*
  - pip:
      - pennylane>=0.45,<0.46
      - "pennylane-lightning-gpu>=0.45,<0.46; platform_system == 'Linux'"
      - "cuquantum-cu12; platform_system == 'Linux'"
      - "cuquantum-python-cu12; platform_system == 'Linux'"
      - "custatevec-cu12; platform_system == 'Linux'"
      - -e .
